export const OAuthController = {
    start: async (req, res) => {
        res.json({ ok: true, message: 'OAuth start not implemented yet' });
    },
    callback: async (req, res) => {
        res.json({ ok: true, message: 'OAuth callback not implemented yet' });
    }
};
